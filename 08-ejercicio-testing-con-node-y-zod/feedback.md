<!-- Aquí irá el feedback del ejercicio -->
Hola! Bueno, primero que nada felicitarte porque hiciste un gran trabajo!
Hicimos unos cuantos cambios:
- Hicimos helpers para simplificar la lectura del código y reducir lineas.
- Cambiamos el sistema de `PUT`, `PATCH` y `DELETE` para que no actualicen los trabajos existentes.
- Agregamos un `ID` con valor `undefined` para que el `JSON.stringify` no genere un error.

EL resto de cambios fueron para evitar errores y pasar a camelCase algunas variables.
Cualquier cosa nos puedes escribir, un saludo y a seguir trabajando!